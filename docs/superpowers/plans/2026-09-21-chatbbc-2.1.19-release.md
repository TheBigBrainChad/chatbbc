# ChatBBC 2.1.19 Release Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish merged PR #8 (bounded rich capture, inert actions, Linux packaging) as ChatBBC 2.1.19 with synchronized metadata, reviewed notes, reproducible artifacts, and no unrelated changes.

**Architecture:** The release commit updates the five version owners checked by `publish.yml`, adds the 2.1.19 changelog section and tag-owned release notes, and leaves bridge protocol 17 unchanged. The tag points at the reviewed release commit on `main`; `publish.yml` builds, smokes, hashes, and publishes every artifact from that tag.

**Tech Stack:** Electron, TypeScript, npm, GitHub Actions, GitHub Releases.

**Spec:** `AGENTS.md` §20 and `.github/workflows/publish.yml`

## Global Constraints

- Release version: `2.1.19`; tag: `v2.1.19`.
- Preserve `APP_TITLE = 'ChatBBC'`, `APP_SLUG = 'chatbbc'`, and bridge protocol `17`.
- Keep production `begin`/`elect`/`arm` non-arming.
- Publish only from the reviewed tag using `.github/workflows/publish.yml`.
- Do not install or mutate the user's live application data.

---

### Task 1: Synchronize release metadata

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `src/main/version.ts`
- Modify: `extension/manifest.json`
- Modify: `AGENTS.md`

**Interfaces:**
- Consumes: merged PR #8 at `a4ecdb3` with protocol 17 and package 2.1.18.
- Produces: five version declarations that all equal `2.1.19`.

- [x] Set the package, lockfile root package, app, and extension versions to `2.1.19`.
- [x] Update the checked source-alignment statement without changing bridge protocol 17.
- [ ] Run the release metadata assertion from `publish.yml` and require exact agreement.

### Task 2: Write reviewed release documentation

**Files:**
- Modify: `CHANGELOG.md`
- Create: `docs/release-notes/v2.1.19.md`
- Create: `docs/worklog-2026-09-21-chatbbc-2.1.19-release.md`

**Interfaces:**
- Consumes: merged PR #8 behavior and the 2026-09-21 source/package evidence.
- Produces: the top changelog entry and the title/notes consumed by `gh release create`.

- [x] Add a 2.1.19 changelog section that does not overclaim live native actions or provider pixels.
- [x] Write release notes whose first `##` heading is the public release title.
- [ ] Record exact local and CI evidence in the release worklog.

### Task 3: Verify, integrate, tag, and publish

**Files:**
- Verify: `.github/workflows/publish.yml`
- Verify: `.github/workflows/release.yml`

**Interfaces:**
- Consumes: the reviewed release commit on `main` and tag `v2.1.19`.
- Produces: one GitHub release with Linux x64/arm64 AppImage and DEB files, extension ZIP, native-source archive, and checksums.

- [ ] Run `npm run verify`, `npm run build`, `npm run verify:tunnel-current`, and `node scripts/check-release-absent.mjs`.
- [ ] Commit the release metadata, open and merge a release PR, and verify the merge commit contains the reviewed release tree.
- [ ] Create and push annotated tag `v2.1.19` at the merged release commit.
- [ ] Dispatch `publish.yml` at `v2.1.19` and wait for every job to pass.
- [ ] Inspect the public release title, tag, target commit, asset names, and checksums before reporting completion.
